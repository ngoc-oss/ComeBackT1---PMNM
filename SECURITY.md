# Báo lỗi và riêng tư

Với lỗi chức năng không nhạy cảm, mở Issue theo mẫu. Không đính kèm ảnh cá nhân chưa ẩn thông tin,
API token hoặc keystore. Khi có repository công khai, bật private vulnerability reporting nếu cần
và bổ sung kênh liên hệ thật của nhóm trước phát hành.

Ứng dụng không gửi ảnh lên server. Lịch sử chỉ ở SharedPreferences trên máy và có nút xóa.
Ảnh chụp lưu tạm trong cache, CSV chỉ chia sẻ khi người dùng bấm xuất và chọn đích.
Xóa dữ liệu ứng dụng sẽ xóa lịch sử; xóa cache sẽ xóa ảnh tạm và CSV.
Không coi dự đoán ngoại quan là chứng nhận an toàn thực phẩm.
